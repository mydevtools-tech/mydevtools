{
  "name": "@mydevtools/desktop",
  "version": "0.1.18",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --bundles app && bash scripts/make-dmg.sh",
    "build:bundle": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
